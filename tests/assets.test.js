import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
test('all GLB dependencies ship with the game',()=>{for(const name of ['race','race-future','sedan-sports','cone']){const file=`public/models/kenney/${name}.glb`,data=fs.readFileSync(file);assert.equal(data.toString('utf8',0,4),'glTF');const gltf=JSON.parse(data.toString('utf8',20,20+data.readUInt32LE(12)));assert.ok(gltf.meshes.length);for(const dependency of [...gltf.buffers,...gltf.images]){if(dependency.uri&&!dependency.uri.startsWith('data:'))assert.ok(fs.existsSync(path.resolve(path.dirname(file),dependency.uri)),`${name}: ${dependency.uri}`);}}});
